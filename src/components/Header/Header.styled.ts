import styled from "styled-components";

export const StyledHeader = styled.header`
  height: 80px;
  background-color: ${(p) => p.theme.colors.whiteAlpha};
  display: flex;
  align-items: center;
`;
